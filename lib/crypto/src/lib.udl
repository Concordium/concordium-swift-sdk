[Error]
enum WalletCryptoError {
    "CallFailed"
};

namespace crypto {
    [Throws=WalletCryptoError]
    string get_account_signing_key(string seed_hex, string net, u32 identity_provider_index, u32 identity_index, u32 credential_counter);
};
